# Copy to backend/.env (or export these in your shell) to override defaults.

# --- Database ---
DATABASE_URL=sqlite:///./data/sanraksha.db

# --- Environmental data sources ---
# true = explicitly simulated demo data; false = require a verified provider.
# Production must keep both false. The app reports unavailable providers and
# retains the last known reading instead of silently manufacturing new values.
USE_MOCK_IMD=true
USE_MOCK_SATELLITE=true
IMD_API_BASE_URL=https://mausam.imd.gov.in/api
IMD_API_KEY=

# --- ML model ---
TRAINED_MODEL_PATH=../ml/models/landslide_model.pkl

# --- Background sync scheduler ---
ENABLE_SCHEDULER=false
SYNC_INTERVAL_MINUTES=30

# --- MQTT / IoT ingest ---
MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=1883
MQTT_TOPIC_PREFIX=sanraksha/sensors
INGEST_TOKEN=dev-ingest-token

# --- Used by iot/gateway/gateway.py ---
BACKEND_URL=http://localhost:8000
